{
  "projectId": "fixeasy-backend",
  "orgId": "emranbd31",
  "settings": {
    "framework": "other",
    "buildCommand": "pip install -r requirements.txt",
    "installCommand": "pip install -r requirements.txt",
    "devCommand": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "outputDirectory": "."
  }
}
