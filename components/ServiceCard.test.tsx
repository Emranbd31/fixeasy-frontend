import { render, screen } from '@testing-library/react';
// import ServiceCard from './ServiceCard'; // Uncomment and adjust path if ServiceCard exists

describe('ServiceCard', () => {
  it('renders service name and image', () => {
    // render(<ServiceCard name="Cleaning" image="/images/service/Cleaning.png" />);
    // expect(screen.getByText(/Cleaning/i)).toBeInTheDocument();
    // expect(screen.getByRole('img')).toBeInTheDocument();
    expect(true).toBe(true); // Placeholder until ServiceCard is implemented
  });
});
